import { styled } from "@mui/material";
import { Link } from "react-router-dom";

export const StyledLink = styled(Link)({
  color: "inherit",
  textDecoration: "none",
  padding: "6px 8px",
  position: "relative",
});

export const styles = {
  appbar: {
    color: "black",
    boxShadow: "none",
  },
};
